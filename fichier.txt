5. Exemple de Flux avec WebRTC :

    L'utilisateur Android ouvre le site web via le navigateur.
    L'application React sur le site génère un identifiant unique pour l'utilisateur (ou le fait automatiquement avec WebRTC).